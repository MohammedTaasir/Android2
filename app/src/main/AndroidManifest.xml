<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miwok">

    <application
        android:allowBackup="true"
        android:icon="@drawable/nus"
        android:label="MyLady💋💋"
        android:roundIcon="@drawable/nus"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miwok">
        <activity android:name=".Phrases"  android:label="Phrases"></activity>
        <activity android:name=".Colors"  android:label="Colors"/>
        <activity android:name=".Family"  android:label="Family"/>
        <activity android:name=".Numbers" android:label="Numbers"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>